{
  "name": "openkai",
  "private": true,
  "version": "0.1.9",
  "description": "OpenKai \u2014 open-source agent harness + TUI with Cortex memory and fusion. Workspace root.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @kaidera/openkai-core && npm run build -w @kaidera/openkai",
    "typecheck": "npm run typecheck -w @kaidera/openkai-core && npm run typecheck -w @kaidera/openkai",
    "test": "npm run build && npm run test -w @kaidera/openkai",
    "clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "~5.9.2"
  },
  "engines": {
    "node": ">=22.19"
  }
}
