# Acceptance matrix — E024 Cortex completion

Environment classes: **dev** (this Mac; shared appliance is observation-only), **clean-kos** (`kos-test`, disposable OpenKai state), **clean-macos** (fresh local macOS account), **scratch** (disposable Cortex appliance), and **managed** (KOS launch with `CORTEX_PROJECT`). Evidence is literal, redacted output under `evidence/`; tokens, administrator credentials, provider keys, and pasted fake secrets never enter shell history or evidence.

## Result and release semantics

- **PASS** — the complete row was observed on the folded `0.1.13` candidate SHA/version in its named environment.
- **FAIL** — the drive completed and an expected observable did not hold.
- **BLOCKED** — a named prerequisite outside the drive is unavailable. A blocker is not a pass or waiver.
- **PARTIAL** — only a strict subset was observed, including historical `0.1.12` evidence.
- **PENDING** — the complete drive has not run.
- **Gate** — must be PASS before version-specific release consent is requested. **Post-publish** — necessarily runs after the consented publication action and must PASS before SHIP closes; failure enters release recovery. **Info** — recorded but not release-blocking.

Historical 2026-09-04 `0.1.12` observations remain baseline evidence and do not satisfy a `0.1.13` candidate gate. Current exact-candidate results below are for source `db7f921658c57e943a763a06bf25312d9ac5eef4`; every other gate row must still finish PASS on that candidate. On a gate row, FAIL, BLOCKED, PARTIAL, or PENDING blocks release. An Info row remains recorded at its observed status but is not release-blocking.

## Clean-host reset contracts

- **KOS:** `ssh kos-test 'rm -rf "$HOME/e024" "$HOME/.omp" "$HOME/.openkai" "$HOME/.config/openkai" "$HOME/.local/bin/openkai" "$HOME/.local/bin/openkai.previous"'`, then install only the named candidate artifact or public channel. Evidence records the candidate SHA, asset SHA-256, host OS/arch, and literal output.
- **macOS:** an administrator interactively creates `openkai-e024` with `sudo sysadminctl -addUser openkai-e024 -home /Users/openkai-e024`; all drives run as `sudo -u openkai-e024 -H ...`; cleanup is `sudo sysadminctl -deleteUser openkai-e024 -secure`. The password is entered interactively and never captured. This row is BLOCKED until an operator with administrator access is present.

## Matrix

| Id | Env | Drive | Expected observable | Candidate evidence | Owner | Relevance | Current |
|---|---|---|---|---|---|---|---|
| A1-KOS | clean-kos | Reset; install from the public `scripts/install.sh` or Homebrew; `openkai --version` | Public installer/checksum succeeds; exact version `0.1.13`; no explicit asset URL bypass | `A1-kos-0.1.13.md` | operator | post-publish | PARTIAL — `A1.md` used an explicit 0.1.12 asset |
| A1-MAC | clean-macos | Create fresh user; install from the other public channel; `openkai --version` | Public installer/checksum succeeds; exact version `0.1.13` | `A1-macos-0.1.13.md` | CTO/operator (D6) | post-publish | BLOCKED — administrator required |
| A2-KOS | clean-kos | Start candidate `openkai` with no keys; execute `/cortex status` in the TUI | Keyless TUI remains running; status names install state, project, writer, rerank, degradation, and exact next action | `A2-kos-0.1.13.md` | operator | gate | PARTIAL — historical 0.1.12 clean-KOS boot only; exact-candidate clean-KOS rerun remains unavailable |
| A2-MAC | clean-macos | Same drive in fresh account | Same complete in-TUI observable | `A2-macos-0.1.13.md` | CTO/operator (D6) | gate | BLOCKED — administrator required |
| A3 | dev | Candidate TUI `/cortex status` with settings `memory.backend=off` and no `CORTEX_PROJECT` | Shared appliance, project `openkai`, registered writer, embedding/rerank state, worker health, and degradation rendered without activating memory | `A3.md` exact-candidate appendix | kai | gate | PASS — actual PTY reported local `:8501`, project `openkai` registered, default writer `kai`, embedding/rerank state, embed worker healthy, and degradation none |
| A4 | clean-macos | Published Cortex installer: `/cortex preflight`, confirm `/cortex install`, `/cortex status`, then uninstall/rollback in `finally` | Preflight and install exit 0; fresh local API becomes healthy; rollback restores absent state | `A4.md` exact-candidate appendix | alpha@kaidera + operator | gate | PARTIAL/BLOCKED — actual candidate PTY mapped the unpublished installer to an actionable GitHub URL with no raw npm E404 or stack; clean-host installation remains unavailable |
| A5 | scratch | `/cortex acceptance start` or typed Cortex lifecycle APIs create/reactivate `openkai-acceptance` and add `probe:probe` | Project 200 and active; exactly the acceptance roster; production project hash unchanged | `A5-0.1.13.md` | kai + Cortex owner | gate | PARTIAL — baseline project remains active |
| A6 | scratch | Select Cortex/project/agent; record a random marker; search it | Search returns marker; production project and rows hash unchanged | `A6-0.1.13.md` | kai | gate | PARTIAL — baseline only |
| A7 | scratch | Prove default `cortexingest.transcripts=false` produces no session row; enable it, end a second session, inspect `cortex-retain --status`; restore setting in `finally` | Default-off/no-ingest; opted-in UUID lands for `probe`; retention is reported; original setting restored | `A7-0.1.13.md` | kai | gate | PARTIAL — opt-in path only |
| A8 | dev | Candidate with no registered/default writer; attempt `cortex_record` through a counting proxy | Refused before payload/POST; message names the exact fix | `A8.md` exact-candidate appendix | kai | gate | PASS — one GET, zero POST and zero request bodies; actionable refusal named how to select a registered writer |
| A9 | scratch | Paste generated fake `sk-…` only into TUI stdin; run `learn` and transcript path; scan captures in `finally` | Secret absent from captured request bodies, Cortex rows, transcript payload, error output, and provider file | `A9-0.1.13.md` | kai | gate | PARTIAL — stored rows only |
| A10 | dev | Settings `memory.backend=off`, no managed env; start candidate session through proxy and run `/memory stats` | Backend remains off; zero Cortex calls through clean exit; existing appliance rows unchanged | `A10.md` exact-candidate appendix | kai | gate | PASS — actual PTY `/memory stats` reported backend off and the proxy counted zero calls through clean exit |
| A10M | managed | Settings remain `off`; launch once with `CORTEX_PROJECT=openkai-acceptance`, then relaunch without it | First status says managed lane active and record/search work; removing env returns to off with zero calls | `A10M-0.1.13.md` | kai | gate | PASS — same isolated home/settings resolved `off`→managed Cortex for status and record/search; removing only the managed variables returned `/memory stats` to off with zero proxy calls; both PTYs exited 0 and final scratch state was worker healthy/project 404 |
| A11 | scratch | In a `finally` path remove `probe`, archive `openkai-acceptance`, and compare production hashes | Acceptance project archived/404; production project/config/rows unchanged | `A11-0.1.13.md` | alpha@kaidera + kai | gate | BLOCKED — Cortex has no archive API |
| A12 | scratch | Snapshot full server config, provider file, health, and degradation; apply `ollama/nomic-embed-text` and rerank unset; restore in `finally` | PATCH 200; model applied; backlog drains; vector-only shown; byte/semantic restoration proof passes | `A12-0.1.13.md` | kai | gate | BLOCKED — scratch config apply/readback/restore passed, but no Ollama model endpoint or non-empty backlog exists |
| A13 | scratch | Same snapshot/restore envelope; apply NVIDIA rerank with operator-provided key | Rerank health set; search reranked; degradation empty; prior state restored | `A13-0.1.13.md` | kai + operator | info | BLOCKED — provider credential unavailable; `A13.md` proves only the rerank-off subcheck, not the normative credentialed drive |
| A14 | scratch | Snapshot; stop embed worker; search and inspect `/memory stats`; restore and recheck | `/workers/health` and backlog authoritatively expose the outage; `/degradation` is supplementary; no silent success; worker and prior state restored | `A14-0.1.13.md` | kai | gate | PASS — worker health returned HTTP 502, backlog grew 0→1, status and stats said the worker signal takes precedence over empty degradation, lexical search returned the marker, worker recovery was healthy, and final reset/up returned project 404 |
| A15 | managed | `CORTEX_PROJECT=openkai-acceptance openkai` with no `memory.backend` edit | UI/status says managed lane active; record/search use acceptance project | `A15.md` exact-candidate appendix | kai | info | PASS — persisted `off` resolved to managed Cortex, record/search succeeded, actual PTY rendered the managed-lane action, and final reset/up returned project 404 |
| A16 | dev fixture | Hash every file under legacy memory root and relevant `agent.db` rows; upgrade `memory.backend=local` plus all legacy keys; hash again | Backend is off; notice shown once; zero `memories.*` settings; all file and DB hashes/rows identical | `A16-0.1.13.md` | kai | gate | PASS — `local` migrated to `off`; retired schema and persisted keys were absent; legacy file hashes and database rows were identical |
| A17 | disposable | Verify newest backup age and checksum/integrity; restore into a disposable appliance; compare project/roster/row counts; destroy it in `finally` | Backup exists within threshold, verifies, restores without touching live state, and restored inventory matches | `A17.md` exact-candidate appendix | KOS/Cortex owner + kai | gate | BLOCKED — archive validation passed until the restore boundary returned `RESTORE_UNAVAILABLE` |
| A18-KOS | clean-kos | Install 0.1.12, run `openkai upgrade --check`, upgrade, verify rollback copy | Witnessed manifest upgrades to exact 0.1.13 and retains rollback | `A18-kos-0.1.13.md` | operator | post-publish | PARTIAL — `A18.md` proved 0.1.10→0.1.12 |
| A18-MAC | clean-macos | Same upgrade drive in fresh account | Same complete observable | `A18-macos-0.1.13.md` | CTO/operator (D6) | post-publish | BLOCKED — administrator required |
| A19C | dev | Candidate release dry-run compares engine/wrapper/runtime versions, generated manifest, installer target input, expected assets, and source provenance | One candidate version/SHA; dry-run exits 0; no public mutation | `A19.md` exact-candidate A19C appendix | kai | gate | PASS — lockstep values were all `0.1.13`; seven binary-target dry-runs plus Kaidera engine/wrapper publish dry-runs exited 0; a temporary installer copy update/verify passed |
| A19 | public channels | After consented publication, compare npm, GitHub release/assets, `latest.json`, tap, and public `scripts/install.sh`; install from both public paths | Every channel reports 0.1.13 and expected checksums; no stale default | `A19-0.1.13.md` | kai | post-publish | BLOCKED — no publication was authorised; public installer remains `v0.1.009` at public tip `a8674ed27e855dc59f3b15277be1ea6989acd4cf` |
| A20 | scratch | Reset/register `openkai-acceptance`; make eight writes across four OpenKai write paths; reset in `finally` | Count delta 8 with 8 unique memory IDs; final appliance up; acceptance project GET 404 | `A20-0.1.13.md` | kai | gate | PASS — scratch `:8601` was resettable; eight writes produced count delta 8 and 8 unique IDs; final reset left the appliance up and the project GET returned 404 |

## Exact-candidate identity and automated validation

- Local source candidate: `db7f921658c57e943a763a06bf25312d9ac5eef4`; final reviewed implementation ancestor: `8aef97f7f4ad2753b0e81627139dec6572270bd2`; canonical public automation tip: `a8674ed27e855dc59f3b15277be1ea6989acd4cf`.
- `bun run check` passed. The candidate suite passed 559 tests across 25 files with 2016 assertions after the security-test environment isolation fix; build, smoke, and `openkai/0.1.13` also passed.
- Ren returned GO with no findings on implementation tip `8aef97f7f4ad2753b0e81627139dec6572270bd2` and re-anchored that GO/no-findings verdict to exact candidate `db7f921658c57e943a763a06bf25312d9ac5eef4`; neither review nor local validation authorises release.
- A19C did not mutate public channels. The public installer is still `v0.1.009`; no tag, push, publish, public installer repoint, or live release occurred.

## Shared mutation rule

The dev appliance may be read but not mutated by A5–A7, A9, or A12–A14. Scratch drives snapshot before the first mutation and restore in `finally`; A20 instead uses its normative whole-scratch reset/register/reset envelope and must finish appliance-up with project GET 404. Restore/reset failure is FAIL and blocks every later mutation. A11 must run even when an earlier acceptance step fails. Direct Cortex database access is forbidden.

## Historical observations — 2026-09-04 host B (cortex-test, Mac scratch appliance :8601)

| Row | Result | Evidence |
|---|---|---|
| A12b real embedding-model change | **FAIL** — PATCH accepted, `/health` reports the new model, but the appliance cannot serve any model except the worker's seeded one; writes stay 200 and `/degradation` stays empty; only a forced backfill surfaces the error | evidence/A12b.md addenda 1–4 |
| A13 rerank-off subcheck | **PARTIAL** — rerank-off loud status only; the credentialed normative A13 drive was not run and remains BLOCKED | evidence/A13.md |
| A14 historical degradation drill (worker down) | **FAIL** under the former `/degradation`-only expectation — `/degradation` stayed empty at 0/20/75 s while `/workers/health` detected the outage and backlog grew | evidence/A14.md |
| A15 source-tip managed lane env-only subcheck | PASS — historical source-tip result; exact candidate independently passed direct and actual-PTY drives with project 404 after cleanup | evidence/A15.md |
| A20 memory retention: N records → N rows (NEW) | **FAIL** — `POST /memory` upserts by `source`; the fork's constant sources keep one row per write path | evidence/A12b.md addendum 4 |
| A1/A2/A18 on Linux, A4 fresh-host install | BLOCKED — kos-test refuses SSH; `@kaidera-ai/cortex` unpublished | handoff 138a96cc (partial) |
