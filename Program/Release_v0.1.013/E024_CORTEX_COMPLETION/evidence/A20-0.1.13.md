# A20 — 0.1.13 distinct-write accounting (scratch Cortex, 2026-09-04)

Candidate: `db7f921658c57e943a763a06bf25312d9ac5eef4`. Target: resettable scratch appliance `http://127.0.0.1:8601`; the shared `:8501` appliance was not addressed. Run ID: `14ce678a-44f2-452a-b208-67d51ff18aaf`.

The drive reset and started the scratch appliance, then registered project `openkai-acceptance` and writer `probe`. Registration and the project read both returned HTTP 200. No credential value was printed or retained in this evidence.

Two uniquely marked records were submitted through each of the four OpenKai paths:

- `cortex-ingest`
- `cortex_record`
- `memory-save`
- `learn`

Observed accounting and search results:

```text
knowledge rows before: 0
knowledge rows after:  8
delta:                 8
writes submitted:      8
unique search IDs:     8
```

Every unique marker was searchable. Cortex search reported `knowledge` as the source label for each result; that value is the stored section label returned by `/search`, not the source string submitted with the upsert.

The `finally` cleanup reset and started the scratch appliance again. A final GET for project `openkai-acceptance` returned HTTP 404, proving the acceptance project and its eight rows were removed.

Result: **PASS (exact candidate)** — eight distinct writes across all four supported paths produced a row-count delta of eight and eight unique searchable IDs. This used only the resettable scratch appliance and does not authorize or claim a release.
