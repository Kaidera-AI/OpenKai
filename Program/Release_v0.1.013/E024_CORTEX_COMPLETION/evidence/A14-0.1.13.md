# A14 — exact 0.1.13 worker-outage reporting, 2026-09-04

- Candidate: `db7f921658c57e943a763a06bf25312d9ac5eef4`.
- Target: disposable Cortex `http://127.0.0.1:8601`, project `openkai-acceptance`, agent `probe`.
- Isolation: the shared `:8501` appliance was not addressed. The scratch administrator token was loaded process-only for the backlog read and registration; bearer-token variables were absent.
- Marker: `E024-A14-8d2f0326-4493-4824-b19c-fdd2f7dd1e25`.

The harness reset and started the scratch appliance, registered `probe:probe`, and observed a healthy worker with zero backlog:

```text
local appliance at http://127.0.0.1:8601 · project openkai-acceptance registered (env) · default writer probe · embedding local/sentence-transformers/all-mpnet-base-v2 · rerank local/cross-encoder/ms-marco-MiniLM-L6-v2 · embed worker: healthy · embedding backlog total: 0 · degradation: none
```

After stopping only `cortex-test_embed-worker_1`, `/workers/health` returned:

```json
{
  "ok": false,
  "status_code": 502,
  "error": "worker unavailable: [Errno -2] Name or service not known"
}
```

A memory row was accepted for deferred enrichment. The administrator backlog then reported `knowledge: 1`, `total: 1`; lexical search returned the marker. `GET /degradation` remained `{"degraded":[]}`, as expected for this Cortex contract. The exact candidate nevertheless rendered the authoritative signals loudly:

```text
local appliance at http://127.0.0.1:8601 · project openkai-acceptance registered (env) · default writer probe · embedding local/sentence-transformers/all-mpnet-base-v2 · rerank local/cross-encoder/ms-marco-MiniLM-L6-v2 · embed worker: down (HTTP 502) — worker unavailable: [Errno -2] Name or service not known · embedding backlog total: 1 · degradation endpoint: none; embed-worker health takes precedence
```

`/memory stats` included:

```text
- embed worker: down (HTTP 502) — worker unavailable: [Errno -2] Name or service not known
- embedding backlog total: 1

## Degradation
- none reported by endpoint; embed-worker health above takes precedence
```

The unconditional restoration returned:

```json
{
  "workerStartExit": 0,
  "resetExit": 0,
  "upExit": 0,
  "projectStatus": 404,
  "finalEmbedOk": true
}
```

**Verdict: PASS.** Worker health and backlog override the supplementary empty degradation list on both status and `/memory stats`; lexical search remains available without a false vector-readiness claim; the worker recovered; and reset left the appliance up without the acceptance project.
