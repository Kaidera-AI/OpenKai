# A19 — channel verify (2026-09-04)

```
npm @kaidera/openkai   0.1.12
npm openkai-engine     0.1.12
GitHub latest release  v0.1.12
latest.json version    0.1.12
tap formula            0.1.12
install.sh default     v0.1.009
```

Result: **FAIL** — five channels agree on 0.1.12; `scripts/install.sh` on product main defaults to `v0.1.009` (a fresh curl install lands on the retired 0.84 line). W3 REWORK: fix the default and add a release-verify step that fails when it lags `latest.json`.

## A19C — exact 0.1.13 candidate, non-mutating channel dry-run

Source candidate: `db7f921658c57e943a763a06bf25312d9ac5eef4`.

Candidate lockstep inspection found `0.1.13` in all required positions:

| Position | Observed |
|---|---|
| engine package | `0.1.13` |
| npm wrapper package | `0.1.13` |
| wrapper engine dependency | `0.1.13` |
| product constant | `0.1.13` |
| lockfile workspace entry | `0.1.13` |
| changelog release heading | `0.1.13` |

The binary release dry-run completed for all seven targets without producing or uploading assets:

```text
darwin-arm64
darwin-x64
linux-x64
linux-arm64
linux-musl-x64
linux-musl-arm64
win32-x64
```

The npm release dry-run reached both the `@kaidera/openkai-engine` engine and `@kaidera/openkai` wrapper at 0.1.13; it planned pack/publish operations only and published nothing. Installer automation was tested only on a temporary copy: update to `v0.1.13` succeeded and the independent verify step accepted it.

The public installer-automation repository remained clean at `a8674ed27e855dc59f3b15277be1ea6989acd4cf`. Its live `scripts/install.sh` default remained `v0.1.009`.

Subcheck result: **PASS (exact candidate, non-mutating A19C)** — source versions, binary planning, npm planning, and temporary-copy installer update/verify are internally consistent.

Normative A19 result: **BLOCKED** — no release consent was given, no public installer was repointed, and no tag, push, npm publish, GitHub release, or other public-channel mutation occurred. The public channels therefore do not yet carry 0.1.13.
