# A11 — cleanup of the acceptance roster (dev Mac, 2026-09-04)

```
$ cortex-remove-agent probe --project openkai-acceptance
{"removed":true,"agent":"probe","project":"openkai-acceptance","history_preserved":true,"message":"Agent 'probe' removed from the openkai-acceptance roster. The agents row and all decisions/lessons/handoffs/messages are preserved; re-activate with cortex-add-agent."}
$ GET /roster (openkai-acceptance)
{"project":"openkai-acceptance","agents":[]}
$ GET /projects/openkai — unchanged
{'project_key': 'openkai', 'status': 'active', 'default_agent': 'kai', 'updated_at': '2026-09-02T18:02:40.139928+00:00'}
```

**Observed limitation:** the API has no project archive/delete route (`PATCH /projects/{key}` accepts only `repo_root`); the `openkai-acceptance` project row and its rows remain (isolated, ~4 test rows). Reported with the W2 findings; the project is reused for future drives (re-add `probe` with `cortex-add-agent`).
