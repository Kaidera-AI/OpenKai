# A15 — managed lane on host B (Mac scratch appliance :8601), 2026-09-04

```
A15 lane: {
  active: true,
  project: "openkai-acceptance",
  source: "env",
  apiUrl: "http://127.0.0.1:8601",
  agent: "probe",
  explicit: true,
}
A15 PASS — managed lane (env only, settings off) recorded and found marker=true
```

## Exact 0.1.13 candidate rerun

- Candidate: `db7f921658c57e943a763a06bf25312d9ac5eef4`.
- Target: disposable Cortex `http://127.0.0.1:8601`, project `openkai-acceptance`, agent `probe`; shared `:8501` was not addressed.
- Authentication: the scratch administrator token was used only by the setup harness to register the project. `CORTEX_API_TOKEN` and `CORTEX_TOKEN` were absent from the OpenKai process.

The direct drive reset and started the scratch appliance, registered `probe:probe`, and observed project HTTP 200. With persisted `memory.backend=off`, managed environment resolution selected `cortex`; `probeCortex` reported project `openkai-acceptance` registered, default writer `probe`, the configured models, and a healthy embed worker. Marker `E024-A15-9d41ed88-3748-4720-b858-97b7aa38bb89` was recorded and returned by search. Its `finally` reset and restarted the appliance and observed project HTTP 404.

The actual compiled-candidate PTY used an isolated home. Setup observed project HTTP 404, registration HTTP 200, then project HTTP 200. `/cortex status` rendered the registered scratch project, default writer `probe`, configured models, healthy worker, and:

```text
managed lane active — remove CORTEX_PROJECT to disable
```

The PTY exited cleanly through `/quit`. Post-PTY cleanup returned:

```json
{
  "mode": "cleanup",
  "resetExit": 0,
  "upExit": 0,
  "projectStatus": 404
}
```

**Verdict: PASS.** The environment-managed lane overrides an `off` persisted setting for the running process, is visible and actionable in the actual TUI, can write/search through the registered writer, and leaves the disposable appliance up without the acceptance project.
