# A10 — settings-driven drive against the released 0.1.12 engine (dev Mac, 2026-09-04)

Script: `bun run e024-w2-drive.ts` in the installed engine dir (imports the released `src/`; every `CORTEX_*` / `OPENKAI_AGENT` env var deleted first). Literal output:

```
A10 PASS — cortexClientFor(off) is undefined
```

Matrix result: **PARTIAL**. This direct 0.1.12 factory check does not prove a candidate session makes zero proxy calls while appliance rows remain byte-for-byte unchanged.

## Exact 0.1.13 candidate — isolated-home actual PTY

Candidate `db7f921658c57e943a763a06bf25312d9ac5eef4` was compiled and run with an isolated home, `memory.backend=off`, and the Cortex URL pointed at a counting proxy on `:8611`. In the actual PTY, `/memory stats` reported that memory was off. The proxy counter remained unchanged through `/quit` and process exit 0:

```json
{"count":0,"requests":[]}
```

Result: **PASS (exact candidate)** — an ordinary backend-off session, including memory status and shutdown, made zero Cortex requests. No shared appliance or public channel was mutated.
