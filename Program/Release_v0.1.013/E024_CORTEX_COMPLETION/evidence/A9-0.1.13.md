# A9 — outbound redaction on the exact candidate (scratch, 2026-09-05)

Candidate: `db7f921658c57e943a763a06bf25312d9ac5eef4`. Environment: scratch
Mac appliance `http://127.0.0.1:8601`; production read-only. Driver:
`evidence/e024-a6a7a9-drive.ts`. The generated fake secret
`sk-e024fake26f46f0b4776436b9003d32bc50b4dc8` (derived from the run UUID) was
placed ONLY into TUI-stdin-equivalent inputs: a `cortex_record` content
string, its `source` string, and the opt-in transcript's user message text.
It was never placed in settings, env, or files. Outbound request bodies were
captured by a process-local `fetch` wrapper inside the driver.

Checks and literal output (final sequence):

```text
A9: stored-rows=2 literal-fake-in-stored=false (must be false)
A9: captured-outbound-bodies=5 literal-fake-in-any-body=false (must be false)
A7: … literal-fake-in-transcript=false (must be false)
```

- Stored Cortex rows (search over the scratch project): the literal fake is
  absent — `redactSecrets` replaces it at the client boundary
  (`client.ts:346`).
- Captured outbound bodies (record, identity, transcript ingest, searches):
  5 bodies, literal fake absent in all.
- Transcript payload: the transcript text stored and searchable, literal fake
  absent (`buildCortexIngestMessages` redacts before `POST /sessions/ingest`).
- Error output: no errors occurred in the drive; the redaction seam
  `redactSecrets` also covers error rendering (`settings.ts:175`).
- Provider file: `grep -r <fake> ~/.openkai` exit 1 (absent); the file still
  carries `providers.embedding = local/embed-v1`, `providers.rerank` unset —
  the drive wrote nothing to it.

Terminal cleanup: reset+up; project 404; `{"projects":[]}`; production
fingerprint unchanged.

Result: **PASS (exact candidate)** — the fake secret crossed no boundary in
literal form: not stored rows, not outbound bodies, not the transcript
payload, not error output, not the provider file.
