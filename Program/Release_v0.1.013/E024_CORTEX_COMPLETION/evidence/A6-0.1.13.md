# A6 — settings-driven record/search on the exact candidate (scratch, 2026-09-05)

Candidate: `db7f921658c57e943a763a06bf25312d9ac5eef4` (worktree
`openkai-fork-e024-retire-local`, branch `e024/w3-retire-local`). Environment:
scratch Mac appliance `http://127.0.0.1:8601` (podman-compose project
`cortex-test`), reset+up via `appliance/dev/cortex-test.sh` (KOS `de446e27`)
before the drive; production `http://localhost:8501` strictly read-only.
Driver: `evidence/e024-a6a7a9-drive.ts` run with `bun`; every `CORTEX_*` /
`OPENKAI_AGENT` env var deleted first; the scratch admin token loaded
process-only from `cortex-test.env` for registration and never printed.

The drive registered `openkai-acceptance` (`POST /projects` with
`X-Cortex-Project-Mode: create-only`, root `/projects/openkai-acceptance`,
roster exactly `probe:probe`, default agent `probe`), then used the
candidate's settings-driven seam — `Settings.isolated({"memory.backend":
"cortex", "cortex.apiUrl": "http://127.0.0.1:8601", "cortex.project":
"openkai-acceptance"})` plus `cortexWriterClientFor` — to record a random
marker and search it back. No `CORTEX_PROJECT` or token env participated in
the client path.

Literal output (run of 2026-09-05, final sequence):

```text
preflight: scratch projects={"projects":[]} (must be [])
preflight: production openkai status=active default_agent=kai
register: POST /projects -> 200
register: GET project -> status=active default_agent=probe
A6: marker=A6-26f46f0b-4776-436b-9003-d32bc50b4dc8 scratch-hits=2 ids=fce85831,fce85831
A6: production hits for marker=0 (must be 0)
finally: production unchanged=true (must be true)
```

The marker was searchable on the scratch project; the same search against the
production `openkai` project returned zero hits; the production project JSON
was byte-identical before and after. Terminal cleanup: `cortex-test.sh reset`
+ `up` (the running scratch image carries no typed project-archive route —
the A11 archive implementation is not in this image), after which
`GET /projects/openkai-acceptance` returned 404 and `GET /projects` returned
`{"projects":[]}`.

Result: **PASS (exact candidate)** — settings-driven record and search
completed against the disposable project with production untouched and
terminal cleanup proven.
