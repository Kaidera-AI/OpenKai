# A1 — fresh install of v0.1.12 by explicit asset URL + sha256 on a clean host (kos-test, 2026-09-04)

```
=== A1: fresh install of v0.1.12 by explicit asset URL + sha256 ===
/home/rocky/e024/openkai-linux-x64: OK
sha256 check exit=0
version: openkai/0.1.12 exit=0
help exit=0
```

Installed at `~/.local/libexec/kaidera-os/openkai/openkai` with the `~/.local/bin/openkai` link (the KOS installer layout). sha256 matches `latest.json`; `openkai --version` → `openkai/0.1.12` exit 0; `openkai --help` exit 0. **PASS.** Note: `scripts/install.sh` was not used because its default still points at v0.1.009 (A19) — the explicit-URL contract given to KOS in `78f86ec5` is what was exercised.
