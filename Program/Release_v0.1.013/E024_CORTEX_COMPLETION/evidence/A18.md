# A18 — upgrade path 0.1.10 → 0.1.12 on a clean host (kos-test, Rocky Linux 10.2 x86_64, 2026-09-04)

Host had no openkai/bun/node and no OpenKai state dirs before the drive. Commands ran over ssh; literal output:

```
=== A18 step 1: install 0.1.010 (omp-linux-x64 asset) ===
installed: openkai/0.1.10 exit=0
=== A18 step 2: openkai upgrade --check ===
update available: 0.1.10 → 0.1.12
exit=0
=== A18 step 3: openkai upgrade ===
upgraded: 0.1.10 → 0.1.12 (previous kept at /home/rocky/.local/bin/openkai.previous; rollback: openkai upgrade --rollback)
exit=0
after upgrade: openkai/0.1.12 exit=0
```

Observed: the 0.1.010 asset (still named `omp-linux-x64`) reports `openkai/0.1.10`; `openkai upgrade --check` reads the witnessed `latest.json` (0.1.12); `openkai upgrade` swaps the binary, keeps `openkai.previous`, names the rollback; `openkai --version` → `openkai/0.1.12`, exit 0. **PASS.**
