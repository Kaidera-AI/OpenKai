# A3 — Cortex status (API-probe form), dev Mac, 2026-09-04

TUI form (`/cortex status`) pending as an operator drive. The probe the command runs was exercised directly against the live appliance:

```
$ curl -s localhost:8501/health
{"status":"healthy","postgres":"connected","event_store":"postgres", …,"version":"2.3","schema_version":"2.3","embed_provider":"local","embed_model":"sentence-transformers/all-mpnet-base-v2","embed_dims":768,"provider_keys":"unconfigured","rerank_enabled":true,"rerank_provider":"openrouter","rerank_model":"cohere/rerank-4-fast","rls_enforced":true}
$ curl -s -H "X-Project: openkai" localhost:8501/projects/openkai/runtime | python3 -c "…agents…"
agents: kai(lead) ren(cpo) quill(knowledge-keeper) beat(pm) cole(orchestrator) bob(full-stack-developer); default_agent: kai; roster.enforce: false
$ curl -s -H "X-Project: openkai" localhost:8501/degradation
{"degraded":[]}
$ openkai config list | grep -E "memory.backend|cortex\."
memory.backend = off (off|local|cortex) · cortex.apiUrl = http://localhost:8501 · cortex.token/project/agent = (not set)
```
Observed: local appliance reachable; project `openkai` registered with default writer `kai`; rerank enabled (openrouter/cohere/rerank-4-fast, `provider_keys: unconfigured` on the appliance side); no degradation; OpenKai memory off.

## Source-tip TUI observation

Source tip `7fe1ece1dd` was compiled and run in the actual TUI with `memory.backend=off` and no `CORTEX_PROJECT`. Executing `/cortex status` rendered:

```
cortex: local appliance at http://localhost:8501 · project openkai registered (settings) · default writer kai · embedding local/sentence-transformers/all-mpnet-base-v2 · rerank openrouter/cohere/rerank-4-fast · degradation: none
```

The appliance was observed only; memory remained off and no write path was activated. Result remains **PARTIAL** because the package still identified as 0.1.12 rather than the folded 0.1.13 candidate.
