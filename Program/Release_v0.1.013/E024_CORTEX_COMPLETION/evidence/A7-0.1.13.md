# A7 — transcript ingest default-off / opt-in on the exact candidate (scratch, 2026-09-05)

Candidate: `db7f921658c57e943a763a06bf25312d9ac5eef4`. Environment: scratch
Mac appliance `http://127.0.0.1:8601`, reset+up before the drive; production
read-only. Driver: `evidence/e024-a6a7a9-drive.ts` (candidate
`CortexIngestController` with a minimal session fake — `subscribe`,
`messages`, `sessionId`, `sessionManager.getCwd`, `model`,
`getPlanModeState`, `isDisposed` — exercising the same `ingestTranscript()`
path the real session-end hook calls); retention reported through the real
`cortex-retain --status` CLI from a scratch workspace bound to
`openkai-acceptance` (`PG_PORT=18602` published via a scratch-only compose
override; DB credentials process-only).

Default-off phase: settings `cortexingest.transcripts=false` —
`ingestTranscript()` returned false and the session UUID was absent from
`GET /sessions/ingested-ids`. Opt-in phase: a second settings instance with
`cortexingest.transcripts=true` — `ingestTranscript()` returned true, the
session UUID appeared in `/sessions/ingested-ids`, and the transcript text
was searchable. Settings restoration holds by construction: both instances
are `Settings.isolated` (in-memory, `#persist=false`), so no settings file was
ever written; the appliance-side restoration is the terminal reset+up.

Literal output (final sequence):

```text
A7: default-off ingestTranscript=false (must be false) off-session-ingested=false (must be false)
A7: opt-in ingestTranscript=true (must be true) session=0aea78f8-70e7-43f8-af2b-2ec22eed02ac session-ingested=true (must be true) transcript-text-stored=true literal-fake-in-transcript=false (must be false)
```

Retention reported (same scratch state, `cortex-retain --status` from
`/tmp/e024-acceptance-ws`):

```text
## Cortex Retention Status — openkai-acceptance
### PostgreSQL (Tier 2 — Active)
  messages: 2 rows (oldest: ?, retain: 90 days)
  team_events: 1 rows (oldest: ?, retain: 90 days)
  decisions: 0 rows (oldest: ?, retain: 365 days)
  lessons: 0 rows (oldest: ?, retain: 365 days)
  handoffs: 0 rows (oldest: ?, retain: 30 days)
```

Terminal cleanup: reset+up; `GET /projects/openkai-acceptance` 404,
`GET /projects` `{"projects":[]}`, production fingerprint unchanged.

Result: **PASS (exact candidate)** — default-off produced no session row;
opt-in landed the named UUID; retention was reported; original (persisted)
settings untouched and the scratch appliance restored.
