# A10M — exact 0.1.13 managed-lane deactivation, 2026-09-04

- Candidate: `db7f921658c57e943a763a06bf25312d9ac5eef4`.
- Target: disposable Cortex `http://127.0.0.1:8601` through counting proxy `http://127.0.0.1:8611`.
- State: one isolated home and one unchanged overlay with `memory.backend=off`, `cortex.apiUrl=http://127.0.0.1:8611`, project `openkai-acceptance`, and agent `probe` were used for both launches.
- Isolation: Cortex bearer/admin variables were absent from both OpenKai PTYs. The shared `:8501` appliance was not addressed.

Scratch setup returned:

```json
{
  "mode": "setup",
  "resetExit": 0,
  "upExit": 0,
  "beforeStatus": 404,
  "registrationStatus": 200,
  "projectStatus": 200
}
```

## Launch 1 — managed environment present

The compiled candidate started in an actual PTY with `CORTEX_PROJECT=openkai-acceptance` and `OPENKAI_AGENT=probe`. `/cortex status` reported the registered project, default writer, configured models, and healthy embed worker, then rendered:

```text
managed lane active — remove CORTEX_PROJECT to disable
```

A direct changed-contract drive using `Settings.isolated({"memory.backend":"off"})` resolved the active backend to `cortex`. It recorded and found marker `E024-A10M-c8f03313-0d4a-4caf-b42b-8398e56353e7`. The proxy observed the four status GETs followed by the two expected write/search calls:

```json
{
  "configuredBackend": "off",
  "activeBackend": "cortex",
  "markerReturned": true,
  "count": 6,
  "requests": [
    "GET /health",
    "GET /projects/openkai-acceptance",
    "GET /degradation",
    "GET /workers/health",
    "POST /memory (247 bytes)",
    "POST /search (68 bytes)"
  ]
}
```

The managed PTY exited through `/quit` with status 0. The proxy counter was then reset to zero.

## Launch 2 — managed environment removed

The same compiled candidate, isolated home, and settings overlay were relaunched in an actual PTY with `CORTEX_PROJECT` and `OPENKAI_AGENT` empty. The welcome frame identified `OpenKai v0.1.13`. `/memory stats` returned:

```text
Memory backend is off — there is nothing to show.
```

While the PTY remained running, the counting proxy returned:

```json
{
  "count": 0,
  "requests": []
}
```

The unmanaged PTY exited through `/quit` with status 0.

## Cleanup

The cleanup harness completed the destructive operations and observed:

```json
{
  "resetExit": 0,
  "upExit": 0,
  "projectStatus": 404
}
```

Its command then exited 1 because the harness's final `/workers/health` assertion made a headerless request whose response supplied no `embed.ok` field. The reset/up and project-404 assertions had already passed. An immediate read with `X-Project: openkai-acceptance` and `X-Agent-Name: probe` returned `workers.embed.ok=true` with the expected embedding/rerank models. This was an evidence-harness defect, not a reset or worker failure; no second reset or product change was needed.

**Verdict: PASS.** With one unchanged persisted `off` setting, the managed variable activated visible Cortex record/search behavior; removing only that variable returned the actual TUI to `off` with zero Cortex calls. Both PTYs exited cleanly, and final scratch state was appliance-up, worker healthy, project 404.
