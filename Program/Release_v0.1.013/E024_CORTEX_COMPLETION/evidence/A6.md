# A6 — settings-driven drive against the released 0.1.12 engine (dev Mac, 2026-09-04)

Script: `bun run e024-w2-drive.ts` in the installed engine dir (imports the released `src/`; every `CORTEX_*` / `OPENKAI_AGENT` env var deleted first). Literal output:

```
A6 connection: project=openkai-acceptance source=settings apiUrl=http://localhost:8501 explicitAgent=false
A6 PASS — marker found in openkai-acceptance=true; hits in production openkai=0
```
