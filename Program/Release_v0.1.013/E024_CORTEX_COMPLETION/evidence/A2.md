# A2 — keyless TUI boot on a clean host (kos-test, Rocky Linux 10.2, 2026-09-04)

```
$ timeout 12 script -qec "openkai" /dev/null > ~/e024/tui.log   # no provider keys, no models.yml, fresh ~/.omp
tui exit=124 (still running at the 12 s timeout) bytes=502164
$ <strip ANSI> ~/e024/tui.log | grep -i "openkai|kaidera|welcome|login|settings|model"
             OpenKai v0.1.12    engine 18.0.11
                                  Welcome back!
 Tip: Press Ctrl+R to reuse a message you wrote before.
           Select provider to login
$ ls -d ~/.omp ~/.openkai
/home/rocky/.omp  /home/rocky/.openkai     (created by the boot; agent.db, history.db, cache)
```

Observed: the 0.1.12 binary boots without any key, paints the OpenKai header with the engine line, and opens the in-TUI **provider sign-in** wizard instead of exiting to the shell. Result: **PARTIAL** — this baseline predates the folded 0.1.13 candidate and did not execute `/cortex status`. It exposed the incorrect first-run “Welcome back!” copy.

## Source-tip defect check

Source tip `7fe1ece1dd` was compiled and launched with an empty temporary HOME/XDG profile and no inherited provider variables. The live TUI remained in the setup wizard and rendered:

```
│      Welcome to OpenKai!       │ # for prompt actions ...
```

The process was stopped cleanly. This proves the first-run copy repair on the source tip; it does not replace either candidate clean-host A2 row.
