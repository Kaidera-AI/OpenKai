# A12 — provider apply through the OpenKai path, idempotent form (dev Mac, 2026-09-04)

Scope amendment: the shared appliance serves the KOS lane, and the local :11434 endpoint is oMLX (no `nomic-embed-text`), so a real embedding-model CHANGE is deferred to the clean host (A12b). This row proves the path end to end without changing behaviour: re-apply the live rerank pair via `applyCortexProviderSelection` with the admin token loaded by the KOS helper into the subshell (never printed).

```
before: {
  embed_provider: "local",
  embed_model: "sentence-transformers/all-mpnet-base-v2",
  rerank_enabled: true,
  rerank_provider: "openrouter",
  rerank_model: "cohere/rerank-4-fast",
}
admin token present in process env: true (never printed)
outcome: {
  fileWritten: true,
  applied: "live",
  detail: "rerank = openrouter/cohere/rerank-4-fast applied to the live appliance",
}
after: {
  embed_provider: "local",
  embed_model: "sentence-transformers/all-mpnet-base-v2",
  rerank_enabled: true,
  rerank_provider: "openrouter",
  rerank_model: "cohere/rerank-4-fast",
}
A12 baseline result: **PARTIAL** — the idempotent 0.1.12 re-apply proved the client path and preserved the appliance, but did not change the embedding model, drain a backlog, or run on the folded 0.1.13 candidate.
provider file slice: {"rerank":{"provider":"openrouter","base_url":"https://openrouter.ai/api/v1","model":"cohere/rerank-4-fast","api_key":"<redacted-or-empty>"}}
```
