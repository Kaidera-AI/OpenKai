# A8 — settings-driven drive against the released 0.1.12 engine (dev Mac, 2026-09-04)

Script: `bun run e024-w2-drive.ts` in the installed engine dir (imports the released `src/`; every `CORTEX_*` / `OPENKAI_AGENT` env var deleted first). Literal output:

```
A8 PASS — refused=true posts=0 gets=1 :: CortexAgentRegistrationError: Cortex cannot verify a writer for project "openkai-acceptance-nowriter". Run /cortex register or set Cortex Agent after registerin
```

Matrix result: **PARTIAL**. The refusal-before-POST subcheck passed on 0.1.12, but must rerun on the folded 0.1.13 candidate through the counting proxy.

## Exact 0.1.13 candidate — local counting mock

Candidate `db7f921658c57e943a763a06bf25312d9ac5eef4` was exercised against a local mock whose project had no registered writer. Literal result:

```json
{
  "refused": true,
  "gets": 1,
  "posts": 0,
  "capturedBodyBytes": 0,
  "message": "CortexAgentRegistrationError: Cortex project \"openkai-acceptance-nowriter\" has no default agent. Run /cortex register or set Cortex Agent to a registered roster name."
}
```

Result: **PASS (exact candidate)** — OpenKai performed the single registration read, refused locally with actionable registration guidance, and sent neither a POST nor request-body bytes. The mock was local; no shared or public service was mutated.
