# A7 — settings-driven drive against the released 0.1.12 engine (dev Mac, 2026-09-04)

Script: `bun run e024-w2-drive.ts` in the installed engine dir (imports the released `src/`; every `CORTEX_*` / `OPENKAI_AGENT` env var deleted first). Literal output:

```
A7 PASS — session 01a0696c-23ec-7000-9995-c18dbf59c4f2 in /sessions/ingested-ids (1 total)
```
